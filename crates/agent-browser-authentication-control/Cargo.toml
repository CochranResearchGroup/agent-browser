[package]
name = "agent-browser-authentication-control"
version = "0.1.0"
edition = "2021"
description = "Provider-free authentication-run control domain for Agent Browser"
license = "Apache-2.0"
repository = "https://github.com/vercel-labs/agent-browser"
publish = false

[dependencies]
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
serde_json = "1.0"
