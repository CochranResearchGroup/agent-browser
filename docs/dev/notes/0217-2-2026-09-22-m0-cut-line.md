# Plan 0217 M0 Executable Cut Line

Date: 2026-09-22

Product lane: PL-PLATFORM

Disposition: active-input

Owning plan or work item: Plan 0217

Related lanes: none

Source baseline: `8995ee0d`

Installed development generation: `0.28.0-1d808efe0797`

Installed executable SHA-256: `1d808efe0797dabdd1da2725d70947ae435a1cbb0a0a2a41fe1ee1ad839b5514`

Provider manifest SHA-256: `177a90339506a8f6bbb44873968923e65e0cac33f3bcbf387a893e50e5d58dd8`

Route inventory SHA-256: `3dc7955d8b8fa7c03ff5eb77b88dc45810f1cc19c3f69467c0b7605220367448`

## Current Baseline

Development runtime status and doctor passed against the installed generation. The provider inventory contains four ready warm routes on displays `:13` through `:16` and two absent elastic routes. Service status projects three `warm_idle` presentation slots. The provider status is ready but blocking, and its top-level `presentationCapacity` is null; current Service status remains the authoritative capacity readback.

The ordinary named-profile dry run is a deterministic no-effect reproducer:

```bash
agent-browser-dev --json --session default --runtime-profile default \
  remote-view open 'data:text/html,<title>P217 M0</title><main>P217-M0-2026-09-22</main>' \
  --view-stream-provider rdp_gateway --dry-run --job-timeout-ms 120000
```

It fails before browser or provider effects with `existing_session_profile_identity_unproven`, axis `profile_lease`, phase `launch_admission`, and `effectState=no_effect`. Current Service State has a released historical `default` session still pointing at profile `p131-controlled-x11`, while the explicitly requested `default` profile is active and shared-local and no `session:default` browser exists. Historical session identity therefore denies a fresh ordinary route even though current presentation capacity exists.

The focused provider-free regression is:

```bash
scripts/ci/rust-tests.sh --focused cold_native_navigation_acquires_child_permission_before_target_binding
```

Before the source repair it failed in 0.07 seconds with `existing_session_profile_identity_unproven`. After the repair it passes in 0.07 seconds. The adjacent `shared_local` selection group passes all nine tests.

## Executable Cut Line

1. `cli/src/native/actions.rs::execute_command` dispatches `remote_view_open` with authenticated route-bound attribution.
2. `cli/src/native/remote_view/open/coordinator.rs::handle_remote_view_open` enters `RouteBoundOpenCoordinator` and builds the direct-open request.
3. `cli/src/native/action_runtime/runtime/profile_lease.rs::service_profile_lease_metadata_for_command` places `remote_view_open` on generic profile admission.
4. `cli/src/native/action_runtime/runtime/daemon.rs::apply_existing_session_profile_selection` previously consulted historical runtime-owner/session evidence before honoring an explicitly selected shared-local profile. Ambiguous or cleanup-owned history returned `existing_session_profile_identity_unproven` before route planning.
5. `cli/src/native/remote_view.rs::plan_remote_view_acquisition` selects a separate available route before stale browser-display fallback. Existing coverage proves an available route wins over stale display metadata and that real checked-out exhaustion remains typed.
6. `cli/src/native/remote_view/open/coordinator.rs::execute_direct_open` owns route acquisition, browser launch or retained attachment, target acquisition, visible-window proof, checkout, handoff persistence, and compensation.
7. `cli/src/native/remote_view_handoff.rs` owns opaque handoff persistence and resolution. Close and route release remain exact-resource operations.

The M1 source cut begins in `apply_existing_session_profile_selection`: an explicit shared-local `remote_view_open` may select the requested profile before ambiguous historical owner metadata only when no current process is proved for that profile. Existing process evidence continues through retained-browser handling. Exact uncertain resources remain protected from cleanup.

## Visual Baseline Status

Protocol and process readiness are positive, but the ordinary path produces no handoff and therefore no operator pixels. Direct X display inspection was refused by display access control on all four ready warm displays. This is a baseline acceptance failure, not visual proof. The first installed candidate that clears ordinary admission must produce the synthetic marker through an authenticated opaque handoff before M0's visual-baseline row can close.

No browser, route, provider, profile, or production mutation occurred during this baseline.

## Recovery Contract Frozen For M2 And M3

<table>
  <thead>
    <tr><th>Case</th><th>Required continuity</th><th>Maximum recovery</th><th>Allowed interruption</th></tr>
  </thead>
  <tbody>
    <tr><td>Browser restart</td><td>Same operator URL, handoff ID, profile, and session; route may change</td><td>90 seconds</td><td>Temporary loss of pixels and input</td></tr>
    <tr><td>Automation runtime restart</td><td>Same operator URL, handoff ID, profile, and session; browser reuse or exact relaunch is allowed</td><td>90 seconds</td><td>Temporary command and input unavailability</td></tr>
    <tr><td>Guacamole connection restart</td><td>Same operator URL and handoff ID; browser, profile, session, route, and display remain exact</td><td>30 seconds</td><td>Temporary viewer disconnect</td></tr>
    <tr><td>XRDP route restart</td><td>Same operator URL, handoff ID, profile, and session; replacement route and display are allowed</td><td>120 seconds</td><td>Temporary viewer and input disconnect</td></tr>
    <tr><td>Development provider restart</td><td>Same operator URL, handoff ID, profile, and session; provider route may be reacquired</td><td>180 seconds</td><td>Temporary presentation unavailability</td></tr>
  </tbody>
</table>

Every recovered case must return current complete pixels and responsive input. URL resolution, process presence, protocol readiness, or a ready state alone does not satisfy the contract.

## Source Qualification

The focused Plan 0217 regression, the nine-test `shared_local` group, the
31-test `remote_view_open` group, the nine-test `profile_selection` group, and
the available-route-over-stale-display acquisition test pass. Workspace
formatting, strict Clippy, remote-view documentation contracts, documentation
links, policy wiring, and the production documentation build also pass.

The comprehensive Rust run completed both lanes in 729 seconds with the native
lane at zero and the support lane at one. Its only failure was the untouched
Lease Authority process-observation test
`browser_launch_completion_derives_an_exact_direct_child_process_identity`,
which observed `Stale` instead of `ExactCurrent`. The exact test then passed
with one test executed, and the complete Lease Authority compartment passed all
118 tests. The broad receipt is therefore retained as non-green evidence; the
bounded reruns support candidate qualification without rewriting that result.

## First Installed Candidate

Candidate `c6358085` built as binary
`2bd55895e03c856e755e5f59fc9d6e8ac3c4f09d89b677ea2a7b53999b67fa3b`
and installed as development generation `0.28.0-2bd55895e03c`. Production was
unchanged, the isolated development skill synchronized, and all three
disposable browser-launch cycles passed.

The installed ordinary no-effect reproducer still returned
`existing_session_profile_identity_unproven`. The first regression had retained
fixture-only `clientSubjectId` and `identityAssurance` fields that the ordinary
CLI request does not carry. The one allowed M1 repair now derives the local
self-declared subject `cli-session:<session>` inside availability-first
`remote_view_open` and evaluates it through the existing shared-local policy.
The corrected test removes both fixture fields and passes, as do all nine
shared-local tests, formatting, and strict Clippy.

Development doctor also exposed an independent acceptance blocker. The durable
inventory still records four ready warm routes on `:13` through `:16`, while
fresh provider observation finds only route 1 on `:24`; routes 2 through 4 have
no observed display. Provider plan and staging succeeded, but preflight fails
closed at `route-keeper-runtime`. Current source deliberately sets both
preflight and live effects to unavailable until runtime-host route-keeper
integration exists, and its tests require that refusal. No provider apply was
attempted. Repairing that interlock would expand into Plan 0211 provider-control
architecture and is outside Plan 0217.

## Replacement Candidate Result

Repair checkpoint `9a03297b` built as binary
`cd33a6aa46d90f93b8d7788840a38db781edffd50e8351ffec05917ffe0bf47b`
and installed as development generation `0.28.0-cd33a6aa46d9` with production
unchanged. The exact ordinary no-effect reproducer still returned
`existing_session_profile_identity_unproven`.

The passing unit seam is therefore insufficient. Generic profile-lease
admission in `service_profile_lease_metadata_for_command` invokes service
profile selection before the remote-view coordinator reaches its later launch
and attribution flow. The request shape at that gate does not satisfy the
attempted availability-first action and identity predicate. The plan's one
implementation attempt and one repair are consumed. No third source repair,
candidate build, browser open, or provider mutation is authorized.

Plan 0217 remains incomplete at two independent gates: the ordinary named
profile is still denied before effects, and the provider cannot establish a
trustworthy four-route acceptance denominator while its durable inventory and
fresh display observations disagree and live provider mutation is protected by
the unfinished route-keeper integration interlock.
