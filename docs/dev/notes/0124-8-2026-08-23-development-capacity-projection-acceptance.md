# Plan 0124 Slice G Development Capacity Projection Acceptance

Date: 2026-08-23

Status: ACCEPTED | LIVE SCALE CYCLE PENDING

Plan: `docs/dev/plans/0124-2026-08-23-scalable-desktop-evidence-and-presentation-capacity-plan.md`

Source commit: `c1ca78e3`

Installed generation: `0.28.0-1d935dbb107f`

Authority: INSTALLED DEVELOPMENT EFFECTS | PRODUCTION READ-ONLY

## Accepted Evidence

- the isolated provider doctor is green with six route identities, six
  distinct Guacamole connection IDs, and four current Xorg displays;
- Service loads the exact provider-owned inventory path only when the
  development launcher supplies it;
- only provider-reported ready routes become Service display, remote-view,
  route-pool, and presentation-slot authority;
- installed Service Status reports four `warm_idle` slots, configured hard
  maximum 6, human reserve 1, recovery reserve 1, and zero binding warnings;
- provider authority reconciliation completed without restarting the provider
  or reopening warm viewers;
- the required development doctor passed after reconciliation;
- local `/guacamole/` returned 200 through dashboard auth and the external path
  redirected to the same-host login boundary;
- development installation and provider reconciliation both reported
  production unchanged.

## Retained Boundary

This packet proves real provider inventory projection, not the remaining
four-to-six lifecycle. The next controlled packet must exercise one human
viewer, concurrent observation and recovery reservations, exact elastic
provisioning, cooldown reclaim, three repeated scale cycles, CDP-only
non-consumption, human-control interlocks, authenticated-browser retention, and
fresh process and pressure readbacks.

Current Service resource pressure reports unowned Agent Browser processes and
approximately 10.6 GB of observed process RSS. Those processes are not cleanup
candidates without positive ownership. The live acceptance must reconcile the
known failed viewer generations and persistent XRDP route-user sessions through
source-owned lifecycle receipts rather than deleting authority files or
restarting shared XRDP.
