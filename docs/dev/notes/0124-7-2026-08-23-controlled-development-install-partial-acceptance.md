# Plan 0124 Slice G Controlled Development Install Partial Acceptance

Date: 2026-08-23

Status: PARTIAL | PROVIDER NAMESPACE BLOCKED

Plan: `docs/dev/plans/0124-2026-08-23-scalable-desktop-evidence-and-presentation-capacity-plan.md`

Source commit: `215d9a8c1d6f662be114d2b9ff6a105f8318598e`

Authority: INSTALLED DEVELOPMENT EFFECTS | PRODUCTION READ-ONLY

## Accepted Development Evidence

- built the release candidate through resource-aware Build Admission with four
  Cargo jobs and active capacity two;
- installed immutable development generation
  `0.28.0-76c3dddafb22` with executable SHA-256
  `76c3dddafb22ecedb948a9392e55665189009781fb5f000d7a2b93c9fcd6bd1d`;
- the development publisher reported `Production unchanged: true`;
- the environment-owned development doctor passed selector, executable, lane,
  unit, process, port, auth, manifest, browser executable, backend, and local
  ingress checks;
- the protected external `agent-browser-dev.ecochran.dyndns.org` dashboard
  completed its authenticated smoke;
- three isolated development browser launch, URL read, close, and no-residue
  cycles passed;
- live CDP tab streaming passed through the `agent-browser-dev` command;
- Service Status, CLI output, and install-doctor JSON returned the exact
  redacted `desktopEvidencePolicy` contract;
- post-smoke development Service State contained zero browsers and zero
  sessions;
- development service GC dry-run reported zero candidates;
- development generation GC removed four unreferenced generations and retained
  the selected generation plus one rollback generation;
- fresh development doctor remained green after every smoke and cleanup.

The production-oriented `install doctor` is not the development environment
health command. Invoking it through the stable development launcher correctly
exposed the compiled policy projection but compared PATH, dashboard, supervisor,
and runtime identities against production conventions and returned unrelated
issues. Installed development health is established by
`pnpm development-runtime:doctor`.

## Unmet Plan 0124 Acceptance

The installed development Service Status has no `presentationCapacity` value.
Plan 0125 intentionally published no development `/guacamole` route and owns no
development Guacamole, XRDP, display, route-user, or presentation-provider
namespace. Therefore Slice G cannot safely:

- provision four distinct warm presentation slots;
- prove concurrent human, observation, and recovery presentation;
- configure and exercise elastic scale-out from four to six;
- perform provider-backed desktop evidence staging, capture, restoration, and
  cooldown scale-in;
- demonstrate provider resource GC returning from six to four.

Using production routes or displays would violate both P125 isolation and this
plan's production read-only boundary. Fabricating Service State rows would test
serialization rather than provider readiness and is not acceptable evidence.

## Exact Remaining Gate

A separately reviewed development presentation-provider deployment is
required. Plan 0127 Slice A now owns the source descriptor, collision guards,
manifest drift doctor, arbitrary six-route inventory, and development-only
skill path. The provider remains explicitly unconfigured. Slice B must create
the development-only Guacamole/XRDP users and connections, displays, route
inventory, privileges, ports, cleanup inventory, and ingress behavior without
reusing production provider state. Once that deployment and readiness doctor
exist, rerun only the unmet Slice G gates against `agent-browser-dev`.

The repository Agent Browser skill contains the accepted CDP-first evidence
guidance. Plan 0127 now publishes that guidance into the development
pseudo-home while the shared installed production skill intentionally remains
unchanged.

Plan 0124 remains in progress. Source Slices A through F are accepted; the
installed development binary, dashboard, ingress, browser, CDP, policy, and GC
boundaries are accepted; provider-backed presentation acceptance is not.
