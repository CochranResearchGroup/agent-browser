# P110 PoC 5 Foundation Stress Source Acceptance

Date: 2026-08-12

Status: SOURCE ACCEPTED | LIVE FOUNDATION EVIDENCE REQUIRED

Implementation: `25e8b746`

Accepted remediation: `b98343f6`

## Outcome

The existing canonical `desktop_interact` action now supports the second named
`p110-foundation-stress-v1` recipe across CLI, HTTP, generic and dedicated MCP,
generated client, schemas, output, dashboard, README, repo skill, and docs.

The source proof establishes one effective cross-ingress session and one
attribution-derived operation principal, durable atomic operation-ledger
transitions, terminal replay after a fresh adapter reload, abandoned
in-progress rejection, principal and request scoped provider effect keys,
actual attempted and acknowledged event receipts, provider-neutral identity,
service-owned ready handoff resolution, immutable safe projections, and a
complete deterministic 25-scenario source matrix.

Individual interaction receipts use `closed_live_evidence_required`. Only the
distinct full-corpus source-acceptance record uses
`planning_open_implementation_blocked`, and it explicitly sets
`liveCapabilityClaim` to false.

## Audit And Remediation

The single fresh audit returned eight accepted findings:

1. the serializable operation ledger was not wired as durable atomic state;
2. cross-ingress principal and effective-session normalization was incomplete;
3. provider keys omitted principal scope and cleanup key attestation drifted;
4. one successful operation incorrectly opened the aggregate entry gate;
5. the stress corpus was incomplete and not output-bound row by row;
6. a provider could author an arbitrary handoff URL;
7. the engine was hard-bound to synthetic provider identity;
8. the dashboard accepted the wrong recipe and non-contract states.

One bounded remediation packet addressed all eight. Closed-world verification
initially kept findings 5 and 6 open. The same packet then added the 25-row
deterministic scenario runner and a `ServiceState`-backed handoff repository
requiring exact browser, session, route, display, handoff state, operator-visible
readiness, and durable URL correspondence. Final closed-world readback passed
all eight findings. No second broad audit was run.

## Primary-Agent Validation

- formatting and strict Clippy: passed;
- focused foundation stress: 4 passed;
- focused desktop interaction: 27 passed;
- cross-ingress `desktop_interact`: 34 passed before the final isolated core
  additions;
- PoC 4 prompt perception: 26 passed;
- PoC 2 locator: 10 passed;
- PoC 1 capture: 26 passed after repairing the test-only dedicated MCP path;
- durable remote-view handoff: 46 passed;
- service-contract metadata: 1 passed;
- full service client: passed;
- API and MCP parity: passed for 100 service-request actions;
- no-launch service-contract smoke: passed;
- dashboard receipt, inspector, workspace, and production-build gates: passed;
- docs production build, WSL Cargo safety, validation selection, and diff
  checks: passed.

The actions architecture structural scan passed. Its aggregate wrapper still
reports pre-existing frozen inventory drift: `dispatch_tests` has 10 tests
where the ledger expects 9, `remote_view_route_tests_two` has 9 where it expects
8, and the aggregate is 265 where it expects 263. PoC 5 did not add either
counted test.

## Boundary

No live browser, installed runtime, display, RDP, Guacamole, X11, CDP,
credential, prompt, challenge, network provider, external process, or OS input
was exercised. Configured production interaction remains
`desktop_input_provider_unavailable` before capture, persistence, authority
lookup, or effects.

All five P110 PoCs are source accepted. P110 live Foundation Acceptance remains
blocked until separately authorized evidence proves an installed production
provider, cross-process event fencing, and one controlled RDP or Guacamole
recipe. Discrete challenge and authentication implementation remains blocked;
only its planning gate is open.
