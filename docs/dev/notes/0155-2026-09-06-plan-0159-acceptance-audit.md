# Plan 0159 acceptance audit

Date: 2026-09-06. Result: the five bounded Plan 0159 criteria are satisfied.
Production delivery is a separate pending decision, as permitted by criterion 4.
This does not accept P158 endurance, every historical failure, or production use
of the new candidate.

| Criterion | Current evidence inspected | Result and boundary |
| --- | --- | --- |
| 1. Original authorized handles and identity guards | All 19 W1 artifact hashes reverified, two original handles recovered after exact disposable host interruption, original owner/process/target retained; foreign-handle denial joins; two real-dispatch owner/process/endpoint counterexample tests pass. | Accepted for the selected recovery and guard seams. No acquisition or replacement target substitutes for continuity. |
| 2. Durable correlated failures and recourse | W1 response/job/event/journal denial join has expected 1, observed 1, missing/duplicate/conflict 0. Two historical journal interruption episodes each have 1/1 with no missing/duplicate/conflict. All 7 artifacts in each episode reverified. All 11 authenticated policy-actor artifacts reverified, with 18/18 projections. Two installed stream-enable successes retain the same listener manifest. Selected primary terminal/response/typed-guard fixes remain in the candidate inventory. | Accepted selected causal joins and source-bound fixes. Journal actor in the W1 episode is joined by request ID; that is not an embedded-actor claim. No universal dashboard/log projection, host reboot or power-loss durability claim. |
| 3. Ordinary authenticated durable links | Protected run 34022233372 has two successful clients, zero retries and all 22 artifact hashes reverified. Human mouse produces white and Enter restores the fixed blue oracle; same-URL reopen and selected concurrency preserve identity. Aggregate canonical-body digest matches. | Accepted ordinary synthetic external pixels/input/reopen. Runtime-convergence notices remain a delivery caveat. No terminal replacement or calibration anchor supplies continuity. |
| 4. Exact repair and delivery decision | Repair inventory 0153 and proposal 0154 separate source, build, P158 installation, production and outcome evidence. Final release binary, materialized support, expected production generation, private 32-file rollback copy, supported dry run, isolated migration diff and source-free installer fixture are verified. | Accepted proposal requirement. Recommendation is to retain production pending the separate decision on 77 explicit profile-policy writes. Deployment, override retirement and future production outcome gates remain pending, not silently waived. |
| 5. Durable private evidence and cleanup | W1 cleanup records zero Service browsers/sessions/tabs and eight ancillary identities accounted for. Inherited residue receipt completes 12/12. Selected episode resource/host exits remain recorded. Current retained synthetic browser has its ready Service row and exact PID/start ticks. Private isolated review/materialization roots contain no browser launch. | Accepted cleanup plus explicitly retained review obligations. Synthetic browser, server and owned warm provider remain retained for the pending delivery review; exact Service closure and PID/start readback are required before another P158 install. No foreign cleanup or global process-absence claim. |

## Readback artifacts and source binding

Private campaign root: `~/.local/state/agent-browser/campaigns/p159/`.
`delivery-evidence-readback.json`, `delivery-logging-readback.json` and
`delivery-retention-readback.json` record this audit. Historical logging artifacts
remain under the P158 campaign in `journal-custody-bj175178`,
`journal-custody-6x48t_o7` and `policy-provenance-f66DTO`. The journal's second seam
is an observer-constructed append-before-retirement filesystem cut, not a claim
that a timed kill intercepted that exact product instruction interval.

W1 evidence remains bound to source `894076b4`; owner guards to `0c4c1c16`.
Selected W3 installed evidence remains bound to `b52f748e`; the final release
candidate is `b3c6b611`. The repair inventory records coupled dependencies and
later focused compatibility corrections. Source ancestry is composition evidence,
not a claim that historical live tests were rerun on the final release binary.

The aggregate's `aggregateSha256` hashes its canonical body without that field;
it is not the serialized file hash. Both hashes are preserved separately in the
audit receipt. An initial readback comparison used the wrong hash definition;
the source-defined canonical-body check corrected it without modifying evidence.

## Validation and delivery status

The final changed surfaces have passing Rust format, workspace clippy with
warnings denied, 25 migration tests, Guacamole asset checks, docs build,
remote-view handoff documentation checks, and the source-free workstation install
fixture against the exact frozen release binary. The takeover helper has its
schema-bound red/green and full client-suite proof. The installed migration
reader rejection was reproduced, repaired, and cleared in the supported dry run.

The validation selector was run against `2bfd47c9`. Host provisioning, fresh VM,
PostgreSQL durability and route-user-sync suggestions are broad installer-path
mappings; those behaviors were not changed by the extension label or migration
reader correction. The reviewed Compose comparison proves only the web service
configuration hash changes. Shared production skill synchronization is excluded
by the isolated-development boundary. No broad live cycle was repeated for audit.

No formal release or whole-branch merge is claimed. The implementation branch
remains the delivery source checkpoint; the production proposal governs the next
separate delivery decision. The frozen candidate must not be deployed under
Plan 0159 alone because its profile-policy writes cross the plan's explicit
production ACL boundary. That pending decision is an outcome of the required
review, not missing execution of the bounded proposal task.


## Subsequent production reconciliation

The original audit accepted bounded recovery on an earlier binary, not reliable
profile use on the final production candidate. Subsequent production smoke
failed and required repair `8bd7f58a`. The repaired release now has its own
original-two-handle recovery proof and installed custom-profile continuity
proof, including headless close/reopen. The delivery record and Plan 0159
checkpoint 47 bind those results. Global production acceptance remains incomplete;
the separate Xvfb failure and doctor findings are not covered by this correction.
